const initiaState = {        firstname: "toom",        lastname: "tam",        age:21,        birthday: "28/07/1996",        email:"toomtamfher@gmail.com"}export default function reducer(state = initiaState, action) {    switch (action.type){        case "SET_USER":{            return {                ...state,                ...action.payload            }        }        default:{            return state        }    }}